{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "scripts": {
    "serve": "cd functions && npm run build && firebase emulators:start --only functions"
  }
}
