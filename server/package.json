{
  "name": "server",
  "version": "1.0.0",
  "main": "functions/lib/index.js",
  "scripts": {
    "install": "cd functions && npm install",
    "build": "cd functions && npm run build",
    "start": "cd functions && npm run build && firebase emulators:start --only functions",
    "serve": "cd functions && npm run serve"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^4.9.0",
    "@types/express": "^4.17.13"
  }
}
